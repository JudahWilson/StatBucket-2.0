TODO
    functionality to easily transfer data between dbs
        support filter of which data
        would be nice to bring all related data over so staging can have related data sometimes
    all tables have fallback col of object storage until db refined